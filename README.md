# Backend application to use with any job interview demos

## Requirements
- node v25
- mongoDB v8.2 running locally on 127.0.0.1. Username and password will be in the `.env`
- Will need to request `.env` and this will need to be put in root folder

## To run
 - `npm install`, first time only
 - `npm start`